// ---------------------------------------------------------------------------
// Deployment config
// ---------------------------------------------------------------------------
// Set this to your deployed backend's base URL (no trailing slash), e.g.
// the Render URL you get after deploying backend/ (see backend/DEPLOY.md).
//
// Local development (backend running on your machine): leave as-is.
// Production: replace with your real backend URL, e.g.
//   window.LM_API_BASE = "https://parakh-backend.onrender.com";
// ---------------------------------------------------------------------------
window.LM_API_BASE = "http://localhost:8000";
